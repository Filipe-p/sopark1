<h1>Spaces#index</h1>
<p>Find me in app/views/spaces/index.html.erb</p>
<div class="container">
        <div class="row">
          <h2 class="text-center">Your next parking spot</h2>
        </div>
        <div class="row">
          <!-- source/index.html.erb -->
          <% @spaces.each do |space| %>
            <div class="col-xs-12 col-sm-4">
              <div class="card">
                <div class="card-category"><%= space.name %> on <%= space.location %></div>
                <div class="card-description">
                  <h2><%= space.price %> € / day</h2>
                </div>

                <%= link_to "", space_path(space), class: "card-link" %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
